document.getElementById("logout_button").addEventListener("click", function() {
    // Get the URL dynamically using Django template tag

    // Redirect to the URL
    window.location.href = logouturl;
});
